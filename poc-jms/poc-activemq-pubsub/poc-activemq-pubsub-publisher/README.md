# poc-activemq-pubsub-publisher

Ejemplo de publicador sobre un topico.

## Compilacion y ejecucion
Compilacion

    $ mvn clean compile assembly:single

Ejecucion

    $ java -jar target/poc-activemq-pubsub-publisher-1.0.0-jar-with-dependencies.jar

